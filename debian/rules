#!/usr/bin/make -f

export DH_GOPKG := github.com/syndtr/gocapability

%:
	dh $@ --buildsystem=golang --with=golang

override_dh_auto_install:
	dh_auto_install
	rm -rf ${CURDIR}/debian/golang-gocapability-dev/usr/bin
